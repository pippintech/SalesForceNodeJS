'use strict';

/*describe('Component: SfaccountComponent', function() {
  // load the controller's module
  beforeEach(module('sforceApp.sfaccount'));

  var SfaccountComponent;

  // Initialize the controller and a mock scope
  beforeEach(inject(function($componentController) {
    SfaccountComponent = $componentController('sfaccount', {});
  }));

  it('should ...', function() {
    expect(1).to.equal(1);
  });
});*/
